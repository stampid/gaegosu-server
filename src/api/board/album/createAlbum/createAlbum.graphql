type Mutation {
  createAlbum(
    title: String!
    content: String!
    boardName: String!
    photo: String!
  ): albumResult!
}

type albumResult {
  success: Boolean!
  board: Board
  err: String
  isLogin: Boolean!
}
