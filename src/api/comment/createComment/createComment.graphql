type Mutation {
  createComment(
    board: Int!
    boardName: String!
    content: String
  ): createCommentResult!
}

type createCommentResult {
  success: Boolean!
  err: String
  isLogin: Boolean!
  comment: Comment
}
